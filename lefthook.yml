pre-commit:
  parallel: true
  commands:
    lint-backend:
      root: packages/backend
      run: npm run lint
    lint-web:
      root: packages/web
      run: npm run lint
    lint-extension:
      root: packages/extension
      run: npm run lint
